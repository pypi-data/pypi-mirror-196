from setuptools import setup
from setuptools import find_packages

setup(
    name='dzmymod',
    version='2',
    description='kk',
    author='Vadim Laukonin',
    packages=find_packages(),
    install_requires=[
        # 0 requries
    ],
)