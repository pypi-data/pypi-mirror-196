from setuptools import setup, find_packages

setup(
    name='queues_master_new',
    version='1.0.2',
    author='itskekoff',
    packages=find_packages(),
    install_requires=['requests'],
)
