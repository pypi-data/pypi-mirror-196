import setuptools

setuptools.setup(
    name = "iosense-connect",
    version = "0.0.1",
    author = "Faclon-Labs",
    description = "iosense connect library",
    packages = ["iosense-connect"]
)