[tool.poetry]
name = "pyassignmentgrader"
version = "0.1"
description = ""
authors = ["CD Clark III <clifton.clark@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
fspathtree = "^0.8"
pytest = "^7.2.2"
pytest-pudb = "^0.7.0"
pyyaml = "^6.0"
typer = {extras = ["all"], version = "0.1"}


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
pygrader = "pyassignmentgrader.cli:app"
