[tool.poetry]
name = "awsglue-local-dev"
version = "1.0.0"
description = "Build Python interfaces to the AWS Glue ETL library for use as a local dependency."
authors = ["Álvaro Brito <alvaro.brito@dataseed.de>"]
packages = [
    { include = "awsglue" },
]
license = "Amazon Software License 1.0"
readme = "awsglue/README.md"
repository = "https://github.com/dataseedug/aws-glue-libs/tree/glue-4.0"

[tool.poetry.dependencies]
python = "^3, < 3.11"
pyspark = "^3.3"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
