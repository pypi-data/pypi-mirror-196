[tool.poetry]
name = "pmc-cli"
version = "0.0"
description = ""
authors = []
packages = [
    { include = "pmc" },
]

[tool.poetry.scripts]
pmc = "pmc.main:run"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
