[tool.poetry]
name = "pydantic-env-settings"
version = "0.1.1"
description = "Pydantic BaseSettings Explicit"
authors = ["Jakub <jjagielka@gmail.com>"]
license = "MIT"
readme = "README.md"
packages = [{ include = "pydantic_env_settings" }]

[tool.poetry.dependencies]
python = "^3.10"
pydantic = "^1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
